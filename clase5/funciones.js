/* function anterior (a) {
    return (a - 1)
}

let resultadoAnterior = anterior(10)
console.log(resultadoAnterior);

function triple (b) {
    return (b * 3)
}

let resultadoTriple = triple(10)
console.log(resultadoTriple);
 */

const toUpper = nombre => nombre.toUpperCase();

console.log(toUpper("romina"));

